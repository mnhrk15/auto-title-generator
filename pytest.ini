[pytest]
testpaths = tests
asyncio_mode = strict

# 実 Gemini API を呼ぶテストはデフォルトで除外する。
# 実行するには `pytest -m integration`（GEMINI_API_KEY が必要）。
markers =
    integration: 実 Gemini API を呼び出すテスト（ネットワークと API キーが必要）

addopts = -m "not integration"
